# Variables de entorno para desarrollo local
# Copiar a .env.local y ajustar según sea necesario

# URL de la API del backend (usada en import.meta.env.VITE_API_URL)
VITE_API_URL=http://localhost:3000
